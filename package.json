{
  "name": "autocensor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.19.0"
  },
  "description": "Revisor local de imágenes con censura automática y aprobación manual.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "prestart": "npm run build",
    "start": "node server.mjs",
    "check": "node --check server.mjs && node --check public/app.js && npm run build"
  },
  "dependencies": {
    "fflate": "^0.8.3",
    "onnxruntime-web": "^1.30.0",
    "preact": "^10.29.8"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.6",
    "vite": "^8.2.2"
  }
}
